# 🚀 Staging deploy runbook

Written 2026-08-12, after the `feat/refactor-revamp` merge landed on `main`
(PR #45, `244e5fb`). Supersedes the "do this first" section of
[`RESUME-HERE.md`](./RESUME-HERE.md), which described that merge as pending.

---

## ⚠️ Check what `DATABASE_URL` points at before every write

`.env` is gitignored, so `DATABASE_URL` is a property of your machine, not of
the checkout — and on this repo it has been seen pointing at **both** a local
database and the shared **staging** one (`forhu-staging-…-postgres`) on the
same day. Nothing in the working tree tells you which is in effect.

If the check below comes back as anything other than `localhost`, these
ordinary commands are writing to a shared database:

| Command                    | What it actually does here                              |
| -------------------------- | ------------------------------------------------------- |
| `npx prisma migrate dev`   | Applies **to staging**, and offers to reset it on drift |
| `npm run db:setup`         | Same — it is `generate && migrate dev`                  |
| `npx prisma migrate reset` | **Drops and recreates staging**                         |
| `npm run db:seed`          | Seeds **staging**                                       |

Check first — this only reads:

```bash
node -e "require('dotenv').config();console.log(process.env.DATABASE_URL?.replace(/:\/\/[^@]*@/,'://***@'))"
```

Always safe: `npx prisma migrate status`, `prisma validate`, `prisma generate`,
`prisma migrate diff`.

> This is why `20260812150000_analytics_events` carries hand-written SQL rather
> than SQL generated by `migrate dev` — when it was authored, `DATABASE_URL`
> pointed at staging, so `migrate dev` would have applied it straight there.

---

## Where staging stands

Staging's `_prisma_migrations` holds **one row, `0_init`**, carrying the
squashed baseline's checksum. The four property/approval migrations that came
in with PR #43 have never been deployed there.

Migrations on `main` today:

```
0_init                                            ✅ applied to staging
20260806150000_house_lot_properties               ❌ not yet
20260806170000_approval_workflows                 ❌ not yet
20260807052331_house_lot_metadata                 ❌ not yet
20260812013548_rename_properties_to_properties_products   ❌ not yet
```

The branch `feat/analytics-events-and-permission-gates` adds a fifth:

```
20260812150000_analytics_events                   ❌ not yet (this branch)
```

The checksum mismatch that used to make `migrate deploy` fail from `main` is
**gone** — that was what merging the refactor branch fixed.

---

## Deploying — use the workflow, not a shell

**`prisma migrate deploy` is already part of both deploy pipelines.** Do not
SSH in and run it by hand: that migrates the database without shipping the code
that matches it, which is the one ordering you never want.

Both `.github/workflows/deploy-staging.yml` and `deploy-production.yml` run, on
the EC2 host, before the app container is replaced:

```bash
docker run --rm --entrypoint npx \
  --network mapanytime --env-file /home/ec2-user/mapanytime-api.env \
  <image>:<sha> prisma migrate deploy
```

Three things that ordering buys:

- the migration runs from **the image being deployed**, so schema and code
  always match
- `DATABASE_URL` comes from the host's env file, never from a checkout
- the script runs under `set -e`, so a failed migration **aborts the deploy**
  and leaves the previous container serving

### To deploy staging

Run the **Deploy Staging** workflow with `workflow_dispatch`, giving it the ref
you want (this branch, or `main`).

> ⚠️ **The automatic staging trigger never fires.** It is wired to
> `workflow_run` on branch `staging`, and **there is no `staging` branch on
> origin** (`git ls-remote --heads origin staging` returns nothing). So merging
> to `main` deploys nothing, and manual dispatch is currently the only path.
> This is the likely reason staging is still sitting on `0_init`.
>
> The job's guard itself is correct —
> `conclusion == 'success' && event == 'push'`, so it will not deploy a red CI
> run once a `staging` branch exists.

### To deploy production

Run the **Deploy Production** workflow — it is `workflow_dispatch` only, with a
`ref` input defaulting to `main`. Nothing deploys to production automatically.
It targets `environment: production`, so any reviewer protection configured on
that environment applies.

### Checking by hand (read-only)

```bash
npx prisma migrate status   # what is pending / applied
```

If it reports a _failed_ or _modified_ migration, stop and resolve that first —
`deploy` will not fix it, and the pipeline will abort on it.

### Locally, after pulling schema changes

```bash
npx prisma generate
```

Symptom of skipping this: `tsc` complains about missing Prisma models
(`merchantAds`, `primaryCategory`, `MERCHANTADKIND`, `analyticsEvents`,
property types). The schema is fine; the generated client is stale. The
deployed containers do not need this — `prisma generate` runs in the image
build.

---

## Telling the team

Anyone with a local database created before the squash has a migration history
that will not match the baseline. They need:

```bash
npx prisma migrate reset
```

**Not `migrate dev`** — it gets confused by the squashed baseline and will
offer its own reset with a more alarming prompt.

Send them this along with it: check your `.env` first. If a teammate's
`DATABASE_URL` also points at staging, `migrate reset` from their machine drops
the shared database.

---

## After this branch merges

`20260812150000_analytics_events` creates the `AnalyticsEvents` table and the
`ANALYTICSEVENTTYPE` enum. Its SQL is hand-written rather than generated by
`migrate dev` — precisely because `migrate dev` would have written to staging
from this checkout — and was verified byte-for-byte against:

```bash
npx prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script
```

It has since been **applied to a real local Postgres and verified**, not just
diffed: 12 columns with the expected nullability, 7 indexes plus the primary
key, all 8 enum values, and 0 foreign key constraints (by design — see the
schema comment). A batch insert, read-back and delete round-tripped, including
`Json` metadata and a null `userId`.

The table is append-only and starts empty, so the migration is fast and carries
no backfill.

The worker gains an `analytics.queue` consumer. It declares its own queue and
DLQ on startup, so no manual RabbitMQ setup is needed — but the worker must
actually be running, or events will pile up in the queue unwritten.
